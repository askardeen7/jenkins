pipeline{
    agent none
    stages {
        stage('Hello'){
           agent {
                node {
                    label 'master'
                }
            }
            steps {
                sh '''
                    ansible --version
                    ansible-playbook --version
                    ansible-galaxy --version
                '''
            }
        }
    }
}
